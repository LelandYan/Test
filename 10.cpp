#include <iostream>
#include <algorithm>
using namespace std;
const int maxn = 30;
vector<int> pile[maxn];




int main()
{
    int q;
    cin >> q;
    string  mp,ov;
    int a,b;
    while(true)
    {
        cin >> mp ;
        if(mp == "quit")
        {
            break;
        }
        cin >> a >> ov >> b;
        if(a == b)
        {
            continue;
        }
        for(int i = 0; i < q; i++)
        {

        }

    }
    return 0;
}
